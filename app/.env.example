# Hugging Face token. Optional: every pinned model repo is public, so downloads
# work without it. Set it to avoid anonymous rate limits or to use gated repos.
# Copy this file to .env (which is gitignored) and fill it in.
HF_TOKEN=
